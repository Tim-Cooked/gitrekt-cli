# Example: Custom Gitrekt Soul

This example demonstrates how to extend the `GitrektSoul` (builtin agent loop) to customize its behavior and use it with the `Shell` UI.

```sh
cd examples/custom-Gitrekt-soul
uv sync --reinstall
uv run main.py
```
